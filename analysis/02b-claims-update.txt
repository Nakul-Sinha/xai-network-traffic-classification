[106] Random-forest encrypted-traffic classification running fully in the Intel Tofino data plane costs 383-398 ns of per-packet processing latency, versus millisecond-level latency for control-plane or split control/data-plane classifiers. This sets the concrete budget any line-rate explanation mechanism must fit inside: post-hoc methods such as SHAP or LIME, which require many forward passes per decis
[107] Classification alone consumes 10-12 of the 12 available match-action stages in the Tofino ingress pipeline, leaving zero ingress stages for any post-inference computation; only the separate egress pipeline's 12 stages remain. An in-band explanation module at line rate would therefore have to be squeezed into egress or offloaded entirely, a hardware constraint that no XAI-for-traffic paper appears 
[108] The authors document an explicit shortcut/leakage artifact in the public encrypted-traffic datasets used (ISCXVPN2016, NIMS IMA, Netflow QUIC): the flow identifier produced by their preprocessing is strongly correlated with the class label and causes over-fitting, so it had to be manually excluded as a feature. This is direct first-hand evidence that these benchmark datasets contain label-correlat
[109] Port numbers function as a quantified shortcut feature in these datasets: an otherwise comparable in-switch model (Flowrest) that additionally uses port numbers, TTL and transport-protocol headers beats the port-free model by only 1.7-3.5% F1, and the authors attribute that entire gap to bias that makes the task artificially easier and that would break under port masquerading. This bounds how much
[110] The authors state as a self-identified limitation that accuracy tops out at 87-95%, which they judge insufficient for high-stakes decisions, and that only tree-based models were deployable in the switch. Line-rate deployment therefore currently forces intrinsically interpretable model classes rather than post-hoc-explained deep models; the paper itself performs no explanation-quality, faithfulness
[111] SHAP's explanation runtime on network IDS models is catastrophically nonlinear and rules out line-rate use for several model families: Table 13 reports SHAP global-explanation cost for 10,000 samples of 0.006 h (LGBM), 0.046 h (RF) and 0.306 h (DNN) but 343 h (SVM), 385 h (MLP), 1512 h (AdaBoost) and 1654 h (KNN); the authors explicitly declare these combinations unusable for real-time IDS. A sepa
[112] E-XAI's 'stability' metric measures only run-to-run repeatability under identical conditions (fraction of top features that intersect across repeated runs on the same data), NOT stability under concept drift or distribution shift — the paper never tests drift. Measured this way, SHAP's global explanations are already highly unrepeatable on CICIDS-2017: Table 11 gives SHAP global stability of 0.28 
[113] After evaluating SHAP and LIME on six metrics across three intrusion datasets and seven AI models, the authors conclude both methods are unfit for deployment, and specifically that NEITHER method is 'complete' — i.e. neither can produce a perturbation-verified valid explanation for all traffic samples, achieving completeness for only one or two attack classes per dataset.
[114] The paper acknowledges a known CIC-IDS dataset-generation artifact — 'Destination Port' being a biased/contaminating feature — yet its own aggregate XAI feature ranking (Table 23) places Destination Port as the rank-1 most important intrusion feature for CICIDS-2017 across all AI models, and TCP_WIN_SCALE_IN (which the authors suspect of the same contaminating effect) as rank-2 for RoEduNet-SIMARG
[115] Adapting the Slack et al. adversarial-scaffolding attack to network intrusion detection, the authors show both SHAP and LIME explanations can be decoupled from the model's actual decision — an adversarial model makes an engineered uncorrelated feature dominate the top-3 ranking while the truly-used biased feature (e.g. flow duration for a DoS sample in CICIDS-2017) is hidden, so an analyst sees a 
[116] CICIDS2017's raw packet captures contain large-scale packet duplication — averaging over 497,000 duplicated packets per day (3.48%–5.96% per day; Monday 514,241/11,709,971 = 4.39%, Thursday 556,013/9,322,025 = 5.96%) — and this duplication corrupts the CICFlowMeter-derived flow features that every published CICIDS2017 classifier is trained on (doubling Bwd Packets/s and Flow Packets/s, and collaps
[117] An entire port scan attack inside CICIDS2017's Thursday infiltration phase — estimated at several tens of thousands of flows (approx. 4,060 flows per Ubuntu host, ~5,300 Windows 7, ~6,900 Mac, ~8,000 per Windows 10, ~12,000 Windows 8.1) — is missing from the ground-truth labels, both in the original CIC CSVs (which label only 36 flows as infiltration) and in Engelen et al.'s 2021 corrected labels.
[118] When the missing port scan is correctly labelled, four standard supervised NIDS models (naive Bayes, SVM, decision tree, random forest) lose on average about 20% of recall and their false positive rate drops by about 3% — i.e. the reported detection performance of the standard CICIDS2017 pipeline is an artifact of the label errors, not of genuine detection capability.
[119] Two independent CICIDS2017 defects (the unlabelled port scan and the ~4.5% packet duplication) cancel each other out, which is why the published literature reports near-perfect scores on this benchmark; the authors conclude such results (e.g. Maseer et al.'s F1/recall/precision > 0.99) cannot be obtained without overfitting to dataset errors.
[120] The authors used model explanations (decision-tree decision paths) as a diagnostic instrument and validated the explanation against an independently established ground-truth cause: the explanation singled out exactly the four features (Flow IAT Min, Flow IAT Mean, Bwd Packets/s, Flow Packets/s) that packet duplication mechanically corrupts, and the paper closes by naming explainability of NIDS fal
[121] The paper states that existing XAI evaluation approaches for intrusion detection systems focus on model-specific properties like fidelity and simplicity while neglecting domain meaningfulness, and that explanation quality in X-IDS is typically not formally measured at all — an open problem stated by the researchers themselves.
[122] The paper proposes three metrics — Feature Alignment Precision (FAP), Feature Alignment Recall (FAR), and Feature Alignment F1 (FAF1) — that validate explanations against a domain-knowledge reference: top-k explanation features are compared to predefined feature sets built by mapping each CICIDS2017 attack class to MITRE ATT&CK offensive techniques and MITRE D3FEND defensive tactics. This is valid
[123] Measured domain-knowledge alignment of SHAP explanations on CICIDS2017 is low in absolute terms and varies strongly by model: at k=5 the DNN reaches FAP 0.30 versus 0.09 for Random Forest, and DNN FAF1 is 0.23/0.24 at k=5/10 versus 0.06/0.07 for RF — i.e., even the best model's top-5 SHAP features are 70% non-aligned with domain-informed feature sets.
[124] The experimental scope is SHAP-only on the CICIDS2017 dataset (with an undersampled balanced subset): SHAP is the sole XAI technique applied to all three IDS models (RF, DNN, CNN-BiLSTM), illustrating SHAP's dominance among explanation techniques actually applied to network intrusion detection.
[125] The authors state a limitation of their own metric design: aggregating FAP and FAR with an arithmetic mean treats every instance as equally important, making the metrics vulnerable to outliers.
[126] Pre-trained raw-byte encrypted traffic classifiers (e.g., ET-BERT, YaTC, NetMamba, netFound, TrafficFormer family) commonly rely on spurious correlations (shortcut learning) that fail to generalize to real-world traffic; the paper proposes BiasSeeker, claimed to be the first model-agnostic, data-driven framework for detecting dataset-specific shortcut features, evaluated on 19 public datasets acro
[127] In widely used public traffic benchmarks (including ISCXVPN2016, USTC-TFC2016, CIC-AndMal2017, CSTNET-TLS 1.3, CrossPlatform, CrossNet2021), flow-identifier fields — source/destination IP addresses, ports, and TLS SNI — rank among the most predictive input fields, i.e., classifiers exploit data-leakage identifiers rather than genuine traffic semantics; the paper categorizes shortcuts into Data-Lea
[128] Occluding the identified shortcut fields measurably degrades reported benchmark accuracy, evidencing that published performance numbers are partly inflated by dataset artifacts: e.g., NetMamba on CrossNet2021 drops from 0.9472 (full features) to 0.9164 with SII removed, and on ISCXVPN2016 from 0.8847 to 0.7821 under random SII masking.
[129] The authors criticize prior XAI/interpretation-based shortcut detection for encrypted traffic (e.g., Trustee-style distillation into interpretable surrogates, Captum/InterpretML attribution, heatmap clustering) as model-specific and poorly transferable across architectures and deployment scenarios — a stated unresolved limitation of explanation-driven auditing in this domain.
[130] The authors state that fully automated shortcut detection in encrypted traffic remains an open problem: BiasSeeker is only semi-automated, final judgments require human inspection, and causal-analysis-based automation is left as future work.
[131] On the same XGBoost intrusion-detection model, statistical (SHAP) and logic-based (VoTE-XAI) feature attributions diverge significantly: features appearing in over 90% of logic-based explanations for an attack class (e.g., dTtL for SlowrateDoS at 98.4%, TotBytes for SYNScan at 91.6%) receive zero SHAP importance, although every top-ranked SHAP feature is contained in the VoTE-XAI explanations. Thi
[132] Across three 5G intrusion datasets (5G-NIDD: 92 features, MSA: 478 features, PFCP: 83 features), logic-based attributions are consistently sparser (e.g., 4-6 vs 16-20 features on PFCP; 27-41 vs 59-90 on MSA) and more stable across samples of the same attack type than SHAP attributions.
[133] Explanation runtime: a single explanation is fast for both methods (SHAP mean ~0.03s per sample; VoTE-XAI ~0.001-0.002s), but computing ALL minimal logic-based explanations per sample takes 0.5-9s on 5G-NIDD and about 17 hours on average on the 478-feature MSA dataset, which the authors flag as a trade-off for real-time security monitoring — i.e., exhaustive provably-correct explanation does not s
[134] The paper's explanation-quality evaluation uses only sparsity, stability, and efficiency as metrics; correctness/faithfulness is argued via formal minimality guarantees of the logic method and semantic alignment with attack mechanics described in prior literature, not validated against independent ground truth or expert user studies — the authors themselves list usability studies and domain-expert
[135] The work is explicitly motivated by recent formal-XAI criticism (Marques-Silva and colleagues) that statistical attribution methods like SHAP and LIME can produce inconsistent and logically unsound attributions, and empirically substantiates part of that criticism in the network-intrusion-detection domain.
[136] Arp et al. define 'spurious correlations' (pitfall P4) as dataset artifacts that create shortcut patterns allowing classifiers to separate classes without learning the actual security problem — the canonical framing of shortcut learning later applied to traffic-classification benchmarks.
[137] In a survey of 30 top-tier security papers (2011-2020), the pitfalls are pervasive: sampling bias is at least partly present in 90% of papers, data snooping in 73%, and every analyzed paper is affected by at least three of the ten pitfalls.
[138] In their network intrusion detection case study, the deep-learning detector Kitsune (autoencoder ensemble) on the Mirai IoT traffic dataset is matched and beaten by a trivial one-feature boxplot detector on packet frequency (simple baseline AUC 0.998 / TPR 0.996 vs Kitsune AUC 0.968 / TPR 0.882), demonstrating that reported deep-NIDS performance can rest on a dataset artifact rather than learned a
[139] The Mirai NIDS benchmark contains a concrete temporal artifact — benign traffic effectively ceases once the attack begins (~74 minutes in) and the infection dominates the capture — making the attack trivially separable and unrepresentative of real deployments where attacks are a tiny fraction of traffic.
[140] The paper prescribes explanation techniques (XAI) as the remedy for detecting spurious correlations in learning-based security systems (demonstrating this with Layerwise Relevance Propagation in a case study), positioning XAI in NIDS primarily as a model-debugging/artifact-discovery tool — while itself relying on, not validating, the faithfulness of those explanation methods.
[141] Explanation faithfulness in security ML is evaluated only by an indirect proxy ('descriptive accuracy': removing top-ranked features and measuring prediction change), not validated against ground-truth explanations, because the authors state direct assessment of the feature-prediction relation is infeasible. Their benchmark (the canonical explanation-quality evaluation in security) covers malware 
[142] Perturbation-based black-box explainers used widely in traffic/security XAI (LIME, KernelSHAP, LEMNA) are unstable even on identical inputs: on average about half of the top-ranked features differ between repeated runs, whereas gradient/propagation-based white-box methods (Gradients, Integrated Gradients, LRP) are deterministic (stability 1.0).
[143] Explanation runtime varies by orders of magnitude across methods: white-box methods (Gradients, IG, LRP) can be computed batch-wise in milliseconds, while the black-box method LEMNA required about one hour for a single large input (530,000-feature DAMD sample) — evidence that perturbation-based explanation is far from line-rate feasible.
[144] Applying explanation methods exposed shortcut learning / dataset artifacts in every deep-learning security system the authors examined: features with no security semantics strongly drive the predictions in all four systems (malware and vulnerability datasets, adjacent to but not including CIC-IDS/ISCX/UNSW traffic datasets).
[145] The authors themselves state as an open problem that the adversarial robustness of explanation methods is unresolved: explanations can be decoupled from predictions by adversarial perturbations, and no guarantees or strong defenses exist as of publication (2020).
[146] Due to CICFlowMeter terminating TCP flows on the first FIN packet (violating RFC 793), CICIDS2017 contains 'TCP appendices' — leftover ACK/FIN fragments recorded as separate labelled flows — which make up 25.9% of the entire dataset, and over 25% of all flows are meaningless artifacts, reaching roughly 50% in several attack categories; the flaw persists in the successor dataset CSE-CIC-IDS2018.
[147] The authors empirically demonstrate shortcut learning on CICIDS2017: a Random Forest classifier used RST-packet presence as a learned shortcut for the Bot class (recognising failed TCP connections rather than botnet behaviour), and used four semantically irrelevant features (Fwd Header Len, Total Fwd Pkt, Min Seg Size Fwd, Init Win bytes Fwd) to differentiate TCP-appendix artifacts between classes
[148] CICIDS2017's labelling strategy assigns attack labels purely from attacker/victim IP addresses plus a time window, without verifying flow content; after applying a payload filter, most 'Web Attack' flows proved empty (Web Brute Force dropped from 1507 to 151 flows, XSS from 652 to 27, Bot from ~2208 to 738), leading the authors to relabel 447,362 flows as 'Attempted' in their corrected dataset.
[149] Aggregated evaluation metrics completely mask these dataset artifacts: overall accuracy and weighted-average precision/recall/F1 stayed at 0.99 across the original, TCP-fixed, and payload-filtered dataset versions, while per-class performance shifted drastically (e.g., Web Attack XSS F1 from 0.18 to 0.79, Bot F1 from 0.60 to 0.99), which is why such errors routinely pass undetected in NIDS researc
[150] The authors explicitly state that reliable detection of shortcut learning remains an open research problem, and position feature-importance analysis (an explainability technique) as the recommended sanity check for any feature-based traffic classifier — in this paper it is the tool by which the spurious correlations were actually discovered.
[151] The paper introduces the Explainability Stability Index (ESI), a scalar [0,1] metric that quantifies TreeSHAP attribution drift under adversarial perturbation, providing a concrete method for evaluating explanation stability (not just prediction robustness) in cybersecurity/network-intrusion classifiers.
[152] Prediction robustness and explanation stability are empirically distinct axes: XGBoost can appear nearly perfectly robust to ZOO attacks (RI ~0.98) while its SHAP explanations drift substantially (ESI ~0.06-0.16), versus ESI 0.14-0.29 for Random Forest — so a robust classifier's explanations can still be unstable.
[153] Adversarial perturbations destabilize the SHAP-based explanations that security analysts use for alert triage, framing explanation instability as an operational threat in deployed security classifiers, not just an academic concern.
[154] The evaluation applies SHAP (TreeSHAP) to tree ensembles (Random Forest, XGBoost) on network intrusion detection datasets including UNSW-NB15, NF-ToN-IoT, and HIKARI-2021, using five attacks including three black-box methods — evidencing that SHAP is the explanation technique of record for tabular NIDS work as of 2026.
[155] Gradient-based black-box attack evaluations can produce degenerate, misleadingly optimistic robustness results on tree models: ZOO reports apparent RI ~0.98 against XGBoost due to piecewise-constant prediction surfaces, while score-based Square Attack exposes genuine vulnerability at RI ~0.36 — a methodological pitfall for robustness/stability benchmarking in this domain.
[156] Local post-hoc explanation methods (LIME, SHAP, LEMNA) are criticized as unsuitable for vetting network-security ML models because, by examining only subsets of individual decisions, they can produce misleading explanations; the paper instead argues for global, high-fidelity decision-tree surrogates.
[157] Explanation faithfulness in TRUSTEE is quantified as agreement between the surrogate decision tree and the black-box model: F1-score between the two models' classifications for classifiers, and R-squared between predictions for regression models; the seven reported case studies achieve fidelity of 0.99-1.00 by this metric.
[158] The ISCX VPN-nonVPN dataset contains a framing artifact — Non-VPN captures retain Ethernet headers while about 90% of VPN captures do not — which a published 1D-CNN encrypted-traffic classifier reporting ~100% precision/recall exploits as a shortcut, deciding VPN vs Non-VPN from three header-offset bytes (B49, B43, B47) unrelated to VPN behavior.
[159] A Random Forest trained on CIC-IDS-2017 detects Heartbleed solely via artifacts of how the trace was generated (attack TCP connections never closed, inflating 'Bwd Packet Length Max' and 'Bwd IAT Total'); on 1,000 realistic out-of-distribution Heartbleed attacks that simply close the connection, its F1 falls from 1.000 to 0.000 — also noting the test split holds only 3 Heartbleed samples.
[160] CIC-IDS-2017 has a topology/OS artifact — attacks originate outside the capture network (Kali Linux, initial TTL 64; DDoS from Windows 8.1, TTL 128) while benign traffic is internal — letting the nPrintML AutoML IDS separate classes from two TTL bits; when tested on real UCSB campus traffic with Suricata-derived labels its average F1 drops from 0.999 to 0.282 (DoS F1 0.000).
[161] Under simulated pure scale drift on NetFlow intrusion-detection data, TreeSHAP attribution distributions remain stable even while the classifier's performance degrades and conventional drift detectors raise alarms — i.e., SHAP explanation stability does not track model performance degradation under scale drift.
[162] Traditional drift detectors can flag changes in feature distributions but cannot determine whether the model's decision boundaries remain valid; the authors propose monitoring SHAP attribution distributions as a way to detect shifts in the feature-to-classification mapping itself.
[163] The evaluation is conducted on two NetFlow-based benchmark datasets (CIC-IDS2018 and UNSW-NB15) with two artificially induced drift regimes (gradual multiplicative drift and an abrupt cross-dataset shift), and explanation-distribution shift is quantified by comparing raw feature distributions against SHAP value distributions using the Kolmogorov–Smirnov test and normalised Wasserstein distance — d
[164] The claimed operational value of the SHAP-distribution monitoring approach is triage between remediation strategies: distinguishing scale drift from concept drift tells operators whether simple preprocessing adjustments suffice or whether costly model retraining is required.
[165] TreeSHAP explanation cost varies by nearly three orders of magnitude across tree-ensemble IDS models: explaining 5,000 samples took 700.759 seconds for Random Forest but only 1.471 seconds for XGBoost on the CICIoT2023-derived corpus, meaning explanation runtime is model-dependent and far from free at deployment scale.
[166] SHAP explanation stability under prediction-preserving input perturbations differs by model: Random Forest showed the strongest base-level explanation stability, while XGBoost — despite the best predictive profile — exhibited greater top-feature turnover and attribution-magnitude drift, i.e., its explanations are less stable even when predictions do not change.
[167] The paper asserts, as a stated criticism of the field, that ML intrusion-detection research treats explanation generation as a computationally free post-processing step and evaluates only accuracy, and argues that operational XAI-IDS must instead be judged on explanation cost, local stability, workload prevalence, and selective invocation.
[168] The raw CICIoT2023 dataset requires explicit artifact mitigation to avoid train/test leakage: the authors had to build a 'leakage-safe' corpus via exact-feature deduplication, label-collision removal, and deterministic hash-level partitioning, implying the unprocessed CIC IoT dataset contains duplicated flows and label collisions that inflate results and can distort explanations.
[169] Selective (policy-based) explanation can recover substantial compute at bounded coverage loss, but the savings collapse under realistic attack-heavy traffic prevalence: ~90% false-negative explanation coverage yielded 28-32% compute savings and ~95% coverage yielded 15-23% on a balanced test, with much smaller savings at natural prevalence.
[170] Local feature attribution methods (post-hoc, model-agnostic, e.g. SHAP-style) assume a stationary environment, and their attributions can become invalid under non-stationarity: explanations do NOT remain stable under concept drift or online model updates.
[171] In streaming/online settings, local attributions are only trustworthy when paired with an explicit change-detection mechanism that flags outdated explanations — standalone post-hoc explanation of a drifting classifier is insufficient.
[172] Continuously recalculating attributions at stream rate is computationally infeasible because attribution methods depend on expensive sampling/permutation approximations; CDLEEDS avoids roughly 80-96% of attribution recalculations (80.05% on Adult, 95.71% on Bank-Marketing, 95.97% on TüEyeQ) by detecting local change from model predictions alone, without computing SHAP values.
[173] CDLEEDS detects both local and global concept drift, achieving the best average score against six drift detectors (ADWIN, DDM, ECDD, MDDM-A, RDDM, ERICS) on recall/false-discovery metrics, but at ~2.32 ms per instance versus ~0.12-0.14 ms for simpler error-rate detectors — an overhead the authors themselves list as a limitation ('CDLEEDS has a higher resource consumption than existing methods for 
[174] This state-of-the-art work on drift-aware explanations was motivated by and evaluated on finance/tabular streams — the only network-security dataset in its evaluation is KDD Cup 1999 (with synthetic drift), and no modern traffic datasets (NSL-KDD, CIC-IDS, UNSW-NB15) or encrypted-traffic data were used — so explanation stability under drift remains unvalidated for contemporary network traffic clas
[175] The CIC-IDS-2017 dataset contains generation artifacts that enable shortcut learning: attacks were generated from hosts outside the collection network (mostly Kali Linux, initial TTL 64; DDoS from Windows 8.1, TTL 128) while benign traffic came from inside hosts, so a reproduced nPrintML IDS with 0.999 F1 separates classes largely via two bits of the IP TTL field; a second artifact (port scans in 
[176] A reproduced 1D-CNN encrypted-traffic classifier on the public ISCX VPN dataset learned a feature-misalignment shortcut (byte B49 is the IPv4 protocol field in VPN samples but the fourth byte of the Ethernet source address in non-VPN samples), and an interventional faithfulness test showed even a fidelity-1.00 three-node explanation was incomplete: tampering the three explained bytes left F1 uncha
[177] TRUSTEE evaluates explanation quality using three quantitative criteria — fidelity (agreement between decision-tree and black-box predictions), complexity (tree size/branch intelligibility), and stability (mean agreement across candidate trees) — and achieved 0.99-1.00 fidelity in all seven case studies (VPN 1D-CNN, CIC-IDS-2017 Random Forest, nPrintML IDS, Kitsune/Mirai, OS fingerprinting, UNSW-I
[178] The authors explicitly criticize local explanation methods (LIME, SHAP, LEMNA, plus PDP and ALE plots) as unsuitable for establishing trust in network-security ML because sample-subset-dependent local explanations can mislead, and they use this to argue for high-fidelity, low-complexity global decision-tree explanations instead.
[179] Surrogate-tree explanations are unstable across identical-condition reruns — in 50 repeated TRUSTEE runs on the Heartbleed use case, some extracted trees had mean agreement as low as about 80%, motivating a stability-selection outer loop — and the authors state as open problems that proving a model does NOT suffer from underspecification 'remains an unsolved problem' and that detection/identificat
[180] The E-XAI paper proposes an end-to-end framework that evaluates the two dominant black-box XAI methods (SHAP and LIME) for network intrusion detection using six metrics — descriptive accuracy, sparsity, stability, efficiency, robustness, and completeness — covering both global and local explanation scopes; this is one of the first systematic explanation-quality benchmarks specific to network IDS.
[181] Faithfulness is evaluated only via a deletion-style proxy ('descriptive accuracy' = model accuracy drop when top-attributed features are removed), not against ground-truth explanations; SHAP beat LIME on this metric, but the accuracy drop appeared predominantly on NSL-KDD — implying the faithfulness signal was weak on CICIDS-2017 and RoEduNet-SIMARGL2021, i.e., removing 'important' features there 
[182] On explanation runtime cost (efficiency), the paper finds a scope split: SHAP is more efficient than LIME for local (single-sample) explanations, while LIME adapted for global explanations is more efficient than SHAP — directly relevant to whether explanation generation is feasible at network line rate.
[183] Under adversarial attacks on the explanations themselves, LIME was found slightly more robust than SHAP (higher attack threshold before explanations are fooled), while on stability (consistency of explanations across repeated runs) the two methods performed roughly the same.
[184] The evaluation spans three benchmark intrusion datasets (RoEduNet-SIMARGL2021, CICIDS-2017, NSL-KDD) and seven AI models (RF, DNN, LightGBM, SVM, KNN, AdaBoost, MLP), and the code is publicly released (github.com/ogarreche/XAI_metrics) as a baseline XAI-evaluation framework for network IDS — but all datasets are offline benchmarks, so concept drift and live-traffic stability are not tested.
[185] LEXNet provides a rare direct faithfulness test of post-hoc XAI in traffic classification: when SHAP and Grad-CAM are applied to the same LEXNet model, they fail to recover the prototypes the model actually used for its decision — top-1 identification accuracy is 8.2% (Grad-CAM) and 5.9% (SHAP), and even using the top-10 predicted regions (25% of the input) accuracy stays below 40% (Grad-CAM 38.9%
[186] Explainable-by-design classification is cheaper at inference than post-hoc explanation in this domain: LEXNet runs at roughly 10,000 classifications/second on CPU (102.7 microseconds/sample), versus about 6k/s for ProtoPNet and about 4k/s for ResNet with Grad-CAM — i.e., built-in prototype explanations cost less runtime than attaching Grad-CAM to a standard CNN, though 10k flows/s is still far fro
[187] The authors themselves state that current deep-learning traffic classifiers fail regulatory faithfulness requirements and that post-hoc explainability methods cannot meet them, motivating explainability-by-design — an explicit researcher-stated criticism of the dominant post-hoc XAI paradigm (SHAP/LIME/Grad-CAM) in networking.
[188] The paper explicitly criticizes the small academic datasets used to evaluate traffic classifiers as unrepresentative of real-world application diversity, and instead evaluates on a commercial-grade dataset of 9.7M flows spanning 200 applications (169 TCP, 31 UDP) collected from four customer deployments in China — supporting the broader concern that academic benchmark results (e.g., ISCX/CIC-style
[189] Explainability-by-design need not sacrifice accuracy in traffic classification: LEXNet matches the accuracy of the best-performing state-of-the-art neural network (ResNet, ~90.4%) while being +4% more accurate, 40% lighter, and 39% faster than ProtoPNet, the prior state-of-the-art explainable-by-design CNN.
[190] On a live 5G testbed (LSTM throughput-prediction xApp at TU Berlin), measured explanation overhead differs by an order of magnitude across XAI methods: SHAP with m=16 samples adds ~15 ms on top of 5.2 ms inference (total ~20.4 ms, ~86% GPU utilization), attention-only adds 0.6 ms, and the proposed hybrid Attention+Integrated-Gradients (k=5) adds 2.8 ms for a total of 8.1 ms at ~73% GPU, which the 
[191] The authors empirically find that raw attention weights are NOT faithful explanations in their network model: attention scored lowest on their fidelity metric and concentrated importance on the throughput feature while neglecting BLER and SINR — a researcher-stated criticism of attention-as-explanation in networking ML.
[192] The paper quantifies why SHAP is impractical at network line rate: its cost scales linearly with the sampling budget (T_xai^SHAP = m·T_inf), so achieving high fidelity on many features forces large m and violates real-time latency budgets in the RAN control loop.
[193] Explanation quality is evaluated via deletion-style faithfulness proxies rather than human ground truth: a local R² between model and explanation surrogate, and an 'explanation precision' Φ = 1 − |ŷ_full − ŷ_top-k|/|ŷ_full| computed by re-running the model on only the top-k attributed features — i.e., faithfulness is measured against the model's own behavior, not against known-correct causes.
[194] The evaluation is narrow with respect to drift and generality: it covers ~100 inference runs of an offline-trained LSTM deployed online against a single synthetic periodic-burst traffic workload (5-feature input {Th, BLER, MCS, RP, SINR}, one COTS UE on a mobile robot), and the paper contains no evaluation of explanation stability under concept drift or long-term monitoring — this is RAN control-l
[195] In-switch random forest inference for encrypted traffic classification on Intel Tofino programmable switches runs with per-packet classification latency of 383-398 nanoseconds, establishing the sub-microsecond compute envelope at line rate; post-hoc explainers such as SHAP or LIME (milliseconds-to-seconds per instance) are orders of magnitude outside this budget, so at line rate only the model's o
[196] Only tree-based models (decision trees and random forests) are currently practical for line-rate deployment inside programmable switches, chosen precisely because of their simple logical structure and limited inference operations - meaning the model family that survives at line rate is the intrinsically interpretable one, not complex models requiring post-hoc XAI.
[197] The deployed classifier uses only nine statistical features derived from packet sizes and inter-arrival times, deliberately excluding port numbers and other header fields because they can be manipulated - an explicit design-time defense against the port-number shortcut features known to inflate results on ISCX-style datasets.
[198] With this minimal size/timing feature set, the in-switch solution reaches weighted F1 of 95.29% on QUIC traffic (365,531 flows), 87.14% on ISCXVPN2016, and 89.82% on NIMS IMA, while consuming under 10% of switch hardware resources - quantifying the accuracy attainable on public encrypted-traffic benchmarks under line-rate constraints.
[199] The only model-understanding analysis performed in this line-rate ETC work is global impurity-based random forest feature importance (e.g., minimum inter-arrival time is the top-ranked feature on ISCXVPN2016 at roughly 15% importance); no per-decision explanation, SHAP/LIME, counterfactual, or explanation-faithfulness evaluation is applied, and the authors themselves flag that 87-95% accuracy 'mig
[200] Supervised, decision-boundary-based explanation methods (the family that includes LIME-style local approximation and perturbation-to-boundary approaches) structurally fail to explain drifting/out-of-distribution samples in security classifiers, because drifting samples lie far from the boundary in a large outlier space; in CADE's experiments boundary-based baselines and COIN got 0%-0.41% of pertur
[201] Explanation quality in this security/drift setting is evaluated with perturbation-based fidelity proxies (latent-distance reduction after substituting selected features, and boundary-crossing ratio), not against human-annotated or ground-truth feature importance: CADE's distance-based explanations reduce the latent distance to the nearest class centroid by 98.8% on Drebin and 79.9% on the IDS2018 
[202] Fidelity proxy metrics disagree across datasets even for the winning method: CADE's own explanations make 97.64% of perturbed drifting samples cross the detection boundary on Drebin but only 1.41% on the IDS2018 network intrusion dataset, showing that boundary-crossing-based faithfulness validation largely fails on network traffic data even when the distance-based metric looks strong.
[203] Explanation generation for network intrusion data runs at seconds-per-sample granularity, orders of magnitude away from network line rate: on IDS2018, explaining a single drifting sample takes 3.2s (CADE), 8.2s (COIN), and 3.7s (boundary-based, plus an additional 76.5s to build the approximation model); drift-detection runs over the IDS2018 experiments averaged 1,422.7s for CADE versus 4,289.3s fo
[204] The authors deliberately excluded LIME and SHAP as explanation baselines for the security drift-explanation task, on the stated grounds that white-box methods outperform black-box ones, so this flagship security-XAI paper provides no empirical evidence about SHAP/LIME behavior on drifting network traffic; they instead propose a custom distance-based explanation and evaluate against random, boundar

(total claim records in journal: 204)

--- orchestrator note ---
[112] SHAP global stability 0.28 on CICIDS2017 (run-to-run, identical conditions) — likely the
E-XAI benchmark paper (Neupane et al.). [114] is the false-confidence phenomenon observed in the
wild: authors KNOW Destination Port is an artifact, their XAI pipeline still ranks it #1 and they
publish the ranking. [115] Slack-style scaffolding ported to NIDS: SHAP/LIME decoupled from the
model's actual decision adversarially. All three strengthen G1's motivation stack.
