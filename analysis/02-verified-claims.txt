[1] Explanations produced by two different XAI tools (TRUSTEE surrogate decision trees and kernel SHAP) disagree with each other for the majority of state-of-the-art DL-based NIDS models tested (2 of 3 usable models; agreement scores reported as HorusEye 0.381, Kitsune 0.167, ENIDrift 0.0, and varying 0
[2] Surrogate-tree explanations of DL-based NIDSs fail their own faithfulness (fidelity) test: TRUSTEE decision-tree explanations achieved negative or zero fidelity against the original models for HorusEye, HELAD and ENIDrift (e.g. ~-1.365 for HorusEye on 30% of the Mirai dataset, ~-1.536e-09 for ENIDri
[3] Generating kernel SHAP explanations for a DL-based NIDS is orders of magnitude too slow for network line rate: 625.70 seconds on average for just 1,000 packet samples along a single root-to-leaf path (~0.63 s/packet, i.e. roughly 1.6 packets/sec), and the analysis of HELAD on 200,000 packets could n
[4] Explanation stability is poor and degrades most severely on the drift-adaptive NIDS: measured as average intersection size of top k=10 features across 3 runs, ENIDrift (the model explicitly designed to handle concept drift via incremental packet embeddings) scored lowest of all models (SHAP 0.133, T
[5] The authors state as an unresolved open problem that both explanation methods are vulnerable to adversarial manipulation and that the degree of that vulnerability has not been quantified; they separately note explanations can leak model information usable for model-inversion attacks, and that the ro
[6] State-of-the-art encrypted traffic classifiers (ET-BERT, YaTC) do not learn intrinsic patterns from encrypted payload content at all — only payload length. Across 72 controlled occlusion experiments, replacing the encrypted payload with pseudo-random values independent of class labels (simulating pe
[7] Widely used benchmark traffic datasets are overwhelmingly composed of UNENCRYPTED traffic despite being used to evaluate 'encrypted' traffic classifiers: ISCXVPN2016 is 98.9% unencrypted, USTC-TFC2016 94.7%, ISCXTor2016 (a Tor dataset) 89.3%, and the Cross-Platform Application dataset 69.7%. This is
[8] Feature occlusion (a model-agnostic faithfulness probe) demonstrates measurable shortcut learning: anonymizing Strong Identification Information (MAC/IP addresses, ports) drops ET-BERT from 0.96 to 0.51 (-0.45) and YaTC from 0.90 to 0.62 (-0.28), an average -0.36. The authors explicitly conclude the
[9] The authors formalize a three-way taxonomy of shortcut learning in network traffic classification — data leakage overfitting (SII/SNI), contextual overfitting (session artifacts like IP ID, header checksum, TCP sequence/ack numbers), and temporal overfitting (TCP timestamp options) — and empirically
[10] ET-BERT's classification is derived ENTIRELY from packet header information, not encrypted payload: header-only occlusion (H1) exactly reproduces its baseline accuracy of 0.63, while encrypted-payload-only occlusion (E1) collapses it to 0.12 (-0.51). This directly refutes the widespread published as
[11] The paper characterizes the 2026 state of XAI for encrypted/network traffic as dominated by post-hoc attribution (SHAP, LIME, attribution-based analyses), and states these methods reveal only which raw inputs appear important while failing to clarify how heterogeneous evidence is internally organize
[12] Traffic-CBM applies concept-based XAI (a Concept Bottleneck Model) to encrypted traffic classification as an ante-hoc / structurally interpretable alternative to post-hoc SHAP/LIME — but its "concepts" are NOT human-defined semantic attributes; they are unsupervised scalar evidence summaries constra
[13] Explanation faithfulness is evaluated only by an internal concept-deletion (necessity/sufficiency) test rather than against any external ground truth: removing the 5 highest-ranked concepts collapses accuracy from 96.02% to 13.44%, and top-k removal degrades performance far more than bottom-k remova
[14] The paper's only attempt to ground concept attributions in something outside the model is an input-perturbation consistency check on timing concepts: flows the model assigns high timing-concept activation are shown to be more sensitive to injected timing perturbations (with prediction flips concentr
[15] The authors themselves state that concept validation and human-centered evaluation of concept-based traffic explanations remain unsolved and are deferred to future work — i.e., as of this 2026 paper no human-subject or expert evaluation of explanation quality was performed for this concept-based tra
[16] This paper is one of the few in the NIDS/traffic domain that validates explanations against an externally defined ground truth: five features nominated by human SOC analysts are used as a ground-truth mask, and Relevance Rank Accuracy (RRA) / Relevance Mass Accuracy (RMA) are computed against that m
[17] On an LSTM alert classifier, the two most widely deployed XAI methods in this literature (SHAP and LIME) score dramatically WORSE on faithfulness than gradient/backprop attribution: DeepLIFT reaches faithfulness correlation 0.7559 +/- 0.2681 and monotonicity 78.35%, versus LIME 0.4209 +/- 0.1835 (59
[18] 'Stability' of explanations in this work is operationalised only as local sensitivity to infinitesimal input perturbations (Max Sensitivity, Euclidean radius 0.1), NOT as stability under concept drift. Despite the data spanning 60 days of live Suricata alerts (Jan-Mar 2022), the paper uses a random 
[19] The paper reports NO runtime, latency, or throughput cost for generating explanations anywhere — the only computational disclosure is the workstation hardware spec, and the entire XAI evaluation is run over just 2000 test data points offline. This is direct evidence that even a dedicated, thorough X
[20] The study deliberately avoids the standard synthetic benchmarks (CIC-IDS/NSL-KDD/UNSW-NB15) in favour of a real production Suricata deployment (60 days, 45,339 hosts, 4401 internal hosts at TalTech SOC) — but then introduces its own artifact by artificially balancing to exactly 10,000 samples per cl
[21] Existing methods for detecting shortcut learning in encrypted network traffic classification are model-specific XAI/interpretation techniques whose validity is bounded by the classifier and the explanation tool chosen — the paper frames this dependence as a methodological weakness and proposes a cla
[22] Standard encrypted-traffic benchmarks contain concrete, enumerable shortcut artifacts in the raw bytes, which the paper sorts into three categories: (1) Data-Leakage Identifiers — source/destination IP, source/destination port, TLS SNI; (2) Relative Artifacts — TCP sequence numbers, acknowledgment n
[23] Masking/occluding these artifact fields causes measurable accuracy loss across all six evaluated datasets, demonstrating the classifiers were materially relying on them rather than on traffic semantics. Reported NetMamba figures include ISCXVPN2016 dropping from 0.8847 (full features) to 0.7821 unde
[24] Shortcut reliance means reported in-distribution benchmark accuracy systematically overstates real-world performance, and the shortcuts are environment-specific rather than causal — e.g. TCP timestamp options that uniquely identify mobile apps in one dataset stop working once timestamps are randomiz
[25] The authors explicitly state that fully automatic shortcut detection remains unsolved, and that image/text XAI techniques do not transfer cleanly to network traffic because of the modality gap between raw binary packets and images or text — their own framework is only semi-automated and is offered a
[26] This 57-page survey (Khan et al., HBKU / QCRI / Munster Tech / Toronto Metropolitan) states that the post-hoc explanation methods dominating explainable IDS (X-IDS) were developed for image processing and are a poor structural fit for cyber/network data, and that feature-attribution and saliency met
[27] The survey's own conclusion identifies an unfilled gap in faithfulness/causal grounding: the X-IDS literature it reviewed focuses predominantly on feature attribution and saliency mapping, but does not establish causality or sensitivity of the attributed features, and the authors call for attribute 
[28] The survey names real-time scalability of explanation over large-scale network data as an open, unresolved concern, framed as an efficiency-vs-explanation-depth trade-off. The treatment is entirely qualitative — the paper reports no explanation-generation latency, throughput, or line-rate overhead m
[29] The survey explicitly declares the problem of explaining temporal and contextual dependencies in IDS to be unsolved, tying it to the evolving nature of threats. Notably, the paper never uses the terms 'concept drift', 'distribution shift', or 'non-stationarity' at all — explanation stability under d
[30] The survey documents that explanation methods are themselves an attack surface in this domain — it catalogues published work using SHAP, LIME and counterfactual explanations to craft evasion, poisoning, membership-inference and model-extraction attacks against IDS and malware classifiers (e.g. Kuppa
[31] More than 25% of all flows in CICIDS2017 are meaningless artefacts with no real identifying characteristics, reaching ~50% for several attack categories; the paper attributes 25.9% of the entire dataset specifically to "TCP appendices" — spurious flows created because CICFlowMeter terminates a TCP f
[32] Feature-importance analysis of a Random Forest on CICIDS2017 identified the exact, named CICFlowMeter features the model exploits as shortcuts rather than attack semantics: four features (Fwd Header Len, Total Fwd Pkt, Min Seg Size Fwd, Init Win bytes Fwd) separate TCP-appendix artefacts of differen
[33] For the Bot class, the classifier's decisive feature was the presence of RST packets — i.e. it detected failed/rejected TCP connections rather than botnet command-and-control behaviour (infected victims were rejected by the botnet master in 2 out of 3 connection attempts). After correcting flow cons
[34] Aggregate accuracy is blind to these artefacts: across all three dataset versions (Original, appendix-fixed Intermediate, payload-filtered Final) the Random Forest reports identical Accuracy = 0.99 and weighted-average Precision/Recall/F1 = 0.99, while per-class F1 shifts dramatically (Bot 0.60 to 0
[35] The authors explicitly state that reliable detection of shortcut learning remains an open research problem, and position feature-importance analysis only as a heuristic 'sanity check' rather than a validated diagnostic — recommending it be combined with per-class metrics and manual inspection of mis
[36] A 1D-CNN encrypted-traffic classifier reporting ~100% precision/recall on the ISCX VPN-nonVPN dataset was shown by decision-tree extraction to decide VPN vs. Non-VPN using only 3 bytes (49, 43, 47) of PCAP/Ethernet/IP metadata that carry no VPN semantics; the root cause is a trace-collection artifac
[37] A Random Forest trained on CIC-IDS-2017 that reproduced the literature's >99% Heartbleed detection results was found to key on a trace-generation artifact (attack TCP connections were never closed between heartbeat messages, inflating 'Bwd Packet Length Max' and 'Bwd IAT Total'); when the authors re
[38] nPrintML's bit-level (4,480-feature) representation on CIC-IDS-2017 exploits spurious correlations rather than attack semantics: after iteratively zeroing out TTL and other dominant fields until only TCP options bits remained, the model still achieved F1 = 0.990, and its port-scan rule reduced to de
[39] Counterfactual validation of the extracted explanation revealed that global surrogate explanations are incomplete even at perfect fidelity: destroying the three identified shortcut bytes (of 748) left the black-box 1D-CNN's accuracy essentially unchanged (0.959 untampered vs. 0.959 tampered) because
[40] Explanations for the same traffic model are not stable across extraction runs, and the authors state there is no rigorous guarantee of non-misleadingness: running TRUSTEE 50 times on the CIC-IDS-2017 Heartbleed model with the stabilization outer loop disabled produced decision trees whose pairwise m
[41] On NetFlow intrusion-detection data, TreeSHAP attribution distributions stay stable under pure scale drift even while classifier performance degrades and conventional feature-distribution drift detectors raise alarms — i.e. SHAP explanation stability is decoupled from model accuracy loss, so an unch
[42] Conventional drift detectors that monitor input feature distributions cannot determine whether a deployed NIDS model's decision boundaries are still valid; the authors position SHAP attribution monitoring as a proxy for shifts in the feature-to-classification mapping itself rather than in the inputs
[43] The evaluation uses synthetically injected drift, not naturally occurring drift: two NetFlow datasets (CIC-IDS2018 and UNSW-NB15) with gradual multiplicative (scale) drift and an abrupt cross-dataset shift, with raw-feature vs. SHAP-value distributions compared by Kolmogorov-Smirnov test and normali
[44] The paper's proposed operational payoff is triage of drift response: comparing SHAP-distribution shift against raw-feature shift is claimed to distinguish drift that a preprocessing/rescaling fix can absorb from drift that requires full model retraining.
[45] The same research group (Bydgoszcz University of Science and Technology) has published two directly relevant prior works cited in this chapter's reference list — a survey of XAI challenges specific to intrusion detection (Artificial Intelligence Review 57(12), 2024) and a critical examination of mul
[46] Prediction robustness and explanation stability are empirically decoupled in security classifiers: XGBoost showed near-perfect robustness under the ZOO attack yet the lowest SHAP explanation stability (ESI 0.06-0.16, where higher = more stable), while Random Forest was less prediction-robust but mor
[47] Perturbations too small to flip the classifier's prediction can still substantially shift SHAP attributions, meaning an analyst-facing explanation can be corrupted without any detectable change in the alert label — a direct threat to explanation reliability in SOC triage of network alerts.
[48] The paper's proposed Explainability Stability Index (ESI) measures attribution drift only with respect to one specific attack (ZOO perturbations at varying epsilon budgets), and the authors concede it is not the drift-maximising attack — so ESI is a lower bound on instability and is attack-dependent
[49] Perturbation-based explanation (KernelSHAP for the MLP) was computationally infeasible on the larger network-flow datasets within the authors' CPU budget, so the neural model was dropped from parts of the evaluation — concrete evidence that model-agnostic SHAP does not scale to realistic flow volume
[50] The authors flag near-perfect clean accuracy (RF 0.998, XGBoost 0.999) on their sampled network datasets as possibly reflecting overfitting or feature leakage rather than genuine class separability — an explicit acknowledgement of dataset artifacts in commonly used flow datasets (UNSW-NB15, NF-ToN-I
[51] On the Kitsune/Mirai IoT NIDS benchmark, a trivial single-feature baseline (boxplot outlier detection on 10-second packet frequency) outperforms the deep autoencoder-ensemble detector KITSUNE at low false-positive rates (TPR 0.996 vs 0.882 at FPR=0.001), demonstrating that the benchmark's attack sig
[52] The authors define shortcut learning in security ('spurious correlations', pitfall P4) using network intrusion detection as the canonical example: a traffic classifier can learn to detect a source IP range that happens to host most attacks in the dataset instead of learning attack behavior, and beca
[53] The paper prescribes explanation techniques (LRP, LEMNA, and Warnecke et al.'s explanation evaluation) as the primary remedy for detecting dataset shortcuts, while explicitly conceding those techniques have known limitations — and its own LRP case study found the resulting explanations hard for a hu
[54] In a review of 30 top-tier security ML papers (CCS, IEEE S&P, USENIX Security, NDSS over 10 years, including 4 network intrusion detection papers), every single paper was affected by at least three pitfalls, individual pitfalls affected 17-90% of papers (sampling bias 90%, data snooping 73% at least
[55] The authors state as an open problem that spurious correlations are inherently hard to identify because they are domain- and objective-dependent (a signal valid in one setting is a shortcut in another), and that bias-correction methods imported from other disciplines are usually inapplicable in secu
[56] The paper defines the first explicit multi-criteria framework for evaluating explanation methods in computer-security ML, comprising six criteria: descriptive accuracy, descriptive sparsity, completeness, stability, efficiency and robustness — and applies it to six explanation methods (Gradients, In
[57] Explanation faithfulness is NOT validated against ground truth in this work; because per-feature ground-truth relevance is unavailable for security models, the authors substitute an indirect deletion-based proxy (descriptive accuracy: remove the k most-relevant features and measure prediction degrad
[58] Explanation methods revealed dataset artifacts / shortcut learning in every one of the four security datasets studied: models achieve high accuracy partly by relying on features with no causal relation to maliciousness (e.g. Mimicus+ PDF features count_trailer and count_box_letter; Drebin+ touchscre
[59] Perturbation/sampling-based explainers (LIME, SHAP, LEMNA) are non-deterministic on security models: repeated explanations of the same unchanged input overlap in fewer than half of their top-ranked features, whereas white-box gradient methods (Gradients, IG, LRP) are deterministic by construction an
[60] Explanation runtime differs by orders of magnitude and is prohibitive for high-throughput deployment: white-box methods (Gradients, LRP) need a single backward pass and run in microseconds-to-milliseconds (e.g. LRP on Mimicus+ ~1.7e-6 s), while LEMNA on the DAMD bytecode CNN needed on the order of 1
[61] Shortcut learning from metadata artifacts is a documented, quantified failure mode in network intrusion datasets: identifier-like fields (IP addresses, port numbers, timestamps, flow IDs, TTL) let classifiers reach near-perfect accuracy without learning attack behaviour — destination port used as th
[62] CIC-IDS2017 and CSE-CIC-IDS2018 — the most-used public NIDS benchmarks — contain concrete, independently replicated defects (duplicated features, duplicated packets, incorrect flow construction, incoherent timestamps, mislabelling, deprecated attack tools) traceable in large part to bugs in the CICF
[63] In the authors' own popularity analysis of 45 NIDS papers published 2020-2023 at CORE A*/A security venues, CIC-IDS2017 was the single most-used public dataset but appeared in only 29% of papers, and the majority of those papers used the original flawed release rather than the available corrected ve
[64] The survey's recommended procedure for detecting dataset artifacts is a single-feature discriminability heuristic combined with post-hoc ML explainability, whose outputs must then be manually reconciled with attack semantics — i.e. XAI is prescribed as an artifact-audit instrument for traffic classi
[65] Most NID datasets ship without explicit train-test splits, so users construct arbitrary (often random or cross-validated) splits; because these ignore time ordering they produce temporal bias and inflated performance, and this compounds with concept/model drift, which the survey names a major obstac
[66] Existing XAI evaluation practice for explainable IDS measures only model-internal properties (fidelity, simplicity, faithfulness, robustness) and does not check whether explanation content is domain-meaningful — i.e., as of May 2025 there was no established way to validate traffic/intrusion-detectio
[67] The paper constructs a proxy ground truth for explanation validation in NIDS by mapping each CICIDS2017 attack class to MITRE ATT&CK techniques, linking those to MITRE D3FEND defensive tactics to derive 'domain-informed feature sets', and then scoring SHAP explanations against them with three new me
[68] Measured against this domain-derived ground truth, SHAP explanations of intrusion detectors on CICIDS2017 align poorly with domain expectations: at the top-5 features precision is 0.09 (Random Forest), 0.30 (DNN) and 0.17 (CNN-BiLSTM), and even at k=40 features the best F1 alignment is only 0.32 (CN
[69] The domain-knowledge 'ground truth' itself is incomplete: for the infiltration and bot attack classes the ATT&CK/D3FEND-derived feature set contains no features at all, producing a structurally zero recall — meaning that for some attack types in CIC-IDS there is no expert-derived reference against w
[70] The authors read the low alignment scores as evidence that the expert-derived feature sets may not transfer to the dataset's engineered flow features, i.e. they treat the mismatch as a limitation of the knowledge base and of the benchmark's feature representation rather than concluding the explanati
[71] The paper proves a formal invalidation result: for any attribution method satisfying the local-accuracy (additivity) axiom — which includes SHAP — every change in the model's decision boundary, whether from an incremental online update or from concept drift, necessarily invalidates the baseline and/
[72] The authors state that keeping local attributions valid in a data stream would require recomputing every past attribution vector at every time step, and that this is infeasible at stream scale. Their own experiment had to be downgraded to a logistic-regression model with the efficient linear-SHAP im
[73] Quantified explanation staleness: using CDLEEDS-triggered recomputation, the fraction of time steps at which a local SHAP attribution actually had to be recalculated ranged from roughly 4% (Bank-Marketing 95.71% reduction; TüEyeQ 95.97%) to roughly 44% (Electricity 56.11% reduction), with Adult at ~
[74] Measured per-observation cost of the change-detection machinery needed to keep explanations fresh: CDLEEDS averaged 2.32 ms per update and ERICS 3.68 ms, versus 0.12–0.14 ms for error-rate-based drift detectors (ADWIN, DDM, ECDD, MDDM-A, RDDM). At 2.32 ms/observation the explanation-maintenance laye
[75] Domain-transfer gap: KDD Cup 1999 (494,020 samples, 41 features, 23 classes) — the only network-intrusion dataset in the study — appears ONLY in the global concept-drift detection benchmark (Tables 2/3: CDLEEDS scored 0.59 ± 0.13 combined recall/(1-FDR), detection delay 5,412 observations). It was e
[76] On the network intrusion dataset (CIC-IDS2018), the conventional 'cross the decision boundary' explanation-fidelity metric essentially collapses: the best method (CADE's distance-based explanation) moves only 1.41% of perturbed drifting samples across the boundary, versus 97.64% on the Android malwa
[77] Supervised, decision-boundary-based explanation methods (the LIME/SHAP/perturbation family) fail structurally on concept-drifted samples, because drifting samples sit far outside the in-distribution region and no small perturbation reaches the boundary, so the explainer receives no gradient signal f
[78] Explanation on network flow data costs seconds per sample, ruling out line-rate deployment: on IDS2018 (83 features per flow), explaining ONE drifting sample takes 3.2s (CADE), 8.2s (COIN), or 3.7s (boundary-based) plus a one-time 76.5s to build the boundary approximation model. The upstream drift-d
[79] The authors explicitly concede that ground-truth explanations are unavailable in this domain, so faithfulness is never validated against ground truth; qualitative validation is done only by manually matching selected features against external human-written threat-analysis reports, and only for the A
[80] The two most widely cited XAI techniques, LIME and SHAP, were deliberately excluded from the explanation baselines; the only comparators evaluated on the network intrusion data are a random-feature baseline, a boundary-based perturbation method, and COIN. The quantitative fidelity comparison on IDS2
[81] Explanation-quality evaluation is almost entirely absent from this literature: from a systematically snowballed corpus of 107 papers on XAI for network traffic analysis (traffic classification, intrusion detection, attack classification, traffic prediction), the authors find only 5 works that define
[82] No datasets with annotated explanation ground truth exist for network traffic — the survey lists this as a missing prerequisite rather than an existing resource, meaning explanations for traffic classifiers have not been validated against ground truth. The authors argue this is structurally harder t
[83] Explanation at operational speed is an unsolved problem: the survey names on-the-fly (real-time) model interpretation inside networking systems as an open challenge requiring system-level support (standard APIs/SDKs) that does not yet exist, and separately notes that current XAI libraries do not exp
[84] The applied-technique inventory is heavily concentrated on two borrowed post-hoc attribution methods: SHAP (in all variants — DeepSHAP, TreeSHAP, KernelSHAP) is the single most frequently used technique, cited in 48 of the 107 surveyed works, with LIME second at 22 works; the two are frequently comb
[85] Explanation stability under evolving traffic is stated as an unresolved gap: because network traffic is non-stationary, NTA models increasingly use incremental/lifelong learning, but the survey states there are no XAI methodologies tailored to incremental training and that conventional XAI tools can
[86] Post-hoc explanation is NOT free at line rate: TreeSHAP on a Random Forest IoT intrusion detector cost 700.759 s for 5,000 samples (140,151.76 microseconds/sample, ~7 explanations/s), versus 1.471 s for XGBoost (294.22 microseconds/sample, 3,398.84 explanations/s) — a ~476x model-dependent gap, both
[87] Explanation faithfulness was explicitly NOT validated against ground truth. The only verification performed was numerical self-consistency of the TreeSHAP additive reconstruction (classification agreement 1.000000), and the authors state outright that this establishes neither causal validity nor sem
[88] CICIoT2023 contains severe dataset artifacts requiring removal before honest evaluation: a 54.8176% exact-duplicate rate across 21.1M unique 39-feature vectors, 533,528 feature-hashes carrying more than one label, and 905 hashes appearing as BOTH benign and attack. Naive random splits therefore leak
[89] Local explanation stability is model-dependent and measurably imperfect even under label-preserving perturbations (epsilon=0.01): XGBoost's mean Top-5 Jaccard was only 0.762-0.804 (i.e. roughly 1 in 4 of the top-5 features churns) versus 0.863-0.907 for Random Forest, while rank correlation stayed h
[90] Selective/triaged explanation is proposed as the mitigation for explanation cost, but yields only modest and prevalence-sensitive savings: 28-32% compute savings at ~90% false-negative explanation coverage and 15-23% at ~95% coverage on a balanced test, with savings 'much smaller' under attack-heavy
[91] This paper performs a rare direct faithfulness validation of post-hoc explanations against ground truth in traffic classification: because LEXNet's prototype layer makes the model's true decision regions known by construction, the authors can score Grad-CAM and SHAP against them. Both post-hoc metho
[92] Post-hoc explanation is quantified as prohibitively expensive at line rate. In Table 7 (CPU inference, microseconds/sample on an Intel Xeon Platinum 8164), ResNet+SHAP costs 6.8E4 us/sample versus LEXNet's 102.7 us/sample — roughly a 660x penalty, i.e. ~68 ms per flow, far outside line-rate budgets.
[93] The authors state that even explainability-by-design imposes an unresolved runtime penalty — a roughly 5x CPU slowdown (102.7 us/sample with the prototype layer vs 20.3 us/sample for the plain ResNet backbone) — and explicitly call this an open challenge, attributing ~80% of the overhead to L2 dista
[94] The paper levels a specific, checkable criticism at the public traffic-classification benchmark corpus: existing evaluations use datasets spanning only 8-80 applications and 8k-950k flows, mostly private, which the authors argue fail to represent real commercial deployments (hundreds to thousands of
[95] On explanation stability under concept drift (sub-question c), the paper offers only a proposal, not a result: the authors suggest that prototype-similarity consistency over time could serve as a supplementary drift-tracking signal alongside conventional drift detection. Notably, no experiment in th
[96] All seven fully-reproducible published network-security ML models the authors re-examined with TRUSTEE were found to suffer from underspecification — every case study in Table 2 is labeled with an inferred inductive bias (shortcut learning, spurious correlations, or out-of-distribution vulnerability
[97] An encrypted-traffic classifier (1D-CNN on the ISCX VPN-nonVPN dataset, published as achieving ~100% precision/recall) was shown to decide VPN vs. non-VPN using non-semantic capture artifacts — the IP protocol field and a byte of the Ethernet MAC addresses of the capture machines — not encrypted-pay
[98] CIC-IDS-2017 contains a documented topological artifact: attack traffic was generated from hosts outside the network and benign traffic from inside, so IP TTL alone separates classes (Kali Linux attackers default TTL 64, Windows 8.1 DDoS host TTL 128). The nPrintML AutoML IDS reproduced at 0.999 F1 
[99] A Random Forest trained on CIC-IDS-2017's 78 precomputed flow features detected Heartbleed perfectly on i.i.d. test data (precision/recall/F1 = 1.000) purely because the generator never closed TCP connections between heartbeat messages, inflating 'Bwd Packet Length Max' and 'Bwd IAT Total'. Regenera
[100] Explanation instability is measured directly and is non-trivial: running TRUSTEE 50 times on the same black-box model and data (with the stabilizing outer loop disabled) produced surrogate trees whose pairwise mean agreement fell to roughly 80% in several iterations, forcing the authors to add an ou
[101] Perturbation-based SHAP is empirically too slow for real-time network inference loops: on an NVIDIA A100 co-located with RAN L1/L2 processing, KernelSHAP with only m=16 samples added ~15 ms of explanation overhead, pushing total per-inference latency from a 5.3 ms baseline to ~20.4 ms (~4x baseline)
[102] Gradient-based and intrinsic explanations are affordable at near-real-time budgets where SHAP is not: Integrated Gradients (k=5 steps) added 2.8 ms of XAI overhead (8.1 ms total, i.e. 52% slower than no-XAI, 73% GPU utilization), and attention-weight extraction added only 0.6 ms (5.9 ms total, +11%,
[103] Attention weights measured the LEAST faithful of the three techniques tested, scoring local-R2 fidelity below 0.4 with large fluctuations, and the authors explicitly conclude raw attention scores cannot be read as faithful explanations. This directly undercuts the common practice in encrypted-traffi
[104] Faithfulness in this paper -- as in the networking-XAI literature generally -- is defined purely as agreement with the model's own outputs, never against ground-truth feature importance: fidelity is a LIME-style local linear surrogate R2 over random perturbations, plus a top-k deletion score Phi = 1
[105] Explanation stability was measured over time under changing network conditions via sliding-window fidelity: standard deviation of local R2 over time was ~0.04 (IG), ~0.05 (attention), ~0.06 (SHAP), with SHAP's fidelity swinging across 0.4-0.7 and attention below 0.4. This is short-horizon operationa

total claims so far: 105
